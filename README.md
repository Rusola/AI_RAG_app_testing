# Local AI- based app for Testing Purposes

It was created for **testing non-deterministic outputs** and exploring model behavior in a controlled local environment.
You would need to **install and run a Large Language Model (LLM) locally**.

The document used for testing can be replaced with any text.  
In this example, the story **"Little Red Riding Hood"** is used 😉.
